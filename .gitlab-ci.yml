
stages:
  - build
  - deploy

build frontend:
  stage: build
  image: node:14.18-alpine
  cache:
    paths:
      - node_modules
  artifacts:
    paths:
      - dist
  tags:
    - frontend-runner
  script:
    - npm install
    - npm run build
    - docker build -t front:latest .

deploy frontend:
  stage: deploy
  only:
    refs:
      - 51-add-certbot-to-docker
      - main # only branch to be deployed
  tags:
    - frontend-runner
  script:
    - sudo docker-compose up -d

